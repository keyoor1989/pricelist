# Database configuration for PriceList application
# Uncomment and configure ONE of the following options

# Option 1: SQLite (simpler setup, good for smaller deployments)
DATABASE_URL="file:./prisma/prod.db"

# Option 2: MySQL (better for production and shared hosting)
# DATABASE_URL="mysql://username:password@hostname:3306/database_name"

# Environment (production is recommended for deployment)
NODE_ENV="production" 